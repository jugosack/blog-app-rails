<%= form_with model: @post, url: user_posts_path(current_user) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class = 'post_form'>
  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.text_field :author_id ,placeholder: 'Author id'%>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_area :title %>
  </div>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_area :text %>
  </div>

  <div>
    <%= form.label :likes_counter, style: "display: block" %>
    <%= form.number_field :likes_counter, placeholder: 'add number' %>
  </div>

  <div>
    <%= form.label :comments_counter, style: "display: block" %>
    <%= form.number_field :comments_counter, placeholder: 'add number' %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<div>

<% end %>
